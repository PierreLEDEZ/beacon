{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the notch and settings windows",
  "windows": ["notch", "settings"],
  "permissions": [
    "core:default"
  ]
}
