{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the notch window",
  "windows": ["notch"],
  "permissions": [
    "core:default"
  ]
}
